{
  "name": "divergent-flow-core",
  "version": "0.4.0",
  "description": "[ARCHIVED] Core business logic and API services for Divergent Flow - ADHD-optimized productivity system. See https://github.com/jgsteeler/divergent-flow-mvp for active development.",
  "deprecated": "This package is archived. Active development has moved to divergent-flow-mvp. See ARCHIVE.md for details.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b -v",
    "build:clean": "tsc -b --clean && tsc -b -v",
    "dev": "cd packages/div-flo-api && nodemon src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "npm run lint --workspaces",
    "clean": "tsc -b --clean",
    "install:all": "npm install",
    "start": "npm run start --workspace=packages/div-flo-api",
    "sync-versions": "node scripts/sync-versions.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.0.14",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "redis": "^5.10.0"
  },
  "overrides": {
    "prisma": "6.17.1",
    "@prisma/client": "6.17.1"
  }
}
