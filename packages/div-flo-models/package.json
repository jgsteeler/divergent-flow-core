{
  "name": "@div-flo/models",
  "version": "0.2.0",
  "description": "Shared interfaces and DTOs for Divergent Flow",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "NODE_OPTIONS='--localstorage-file=./.jest-localstorage' jest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "prisma": "6.17.1",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "6.17.1"
  }
}
